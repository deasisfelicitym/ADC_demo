# ADC_demo
This program uses an LDR (photoresistor) to detect the brightness of the surroundings. The ADC reads the LDR value and divides the brightness into three levels, with each level controlling a different LED color: green, yellow, or red.
